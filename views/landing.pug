include partials/landingheader

block content
	//- movie details 
	if result.length !== 0
		div(class="flexstretch")
			div(class="flexitem moviequotelarge" id="moviequote") "#{result[quiz].moviequote}"

		div(class="flexcontainer" )
			div(class="flexitem movie-name-year" id="movie-name" style="visibility: hidden;") #{result[quiz].moviename}, #{result[quiz].year} - #{result[quiz].screenwriter}

		//- display information to user 
		div(class="flexcontainer" id="message-block" style="visibility: hidden;")
			div(class="flexitem")
				h4(id="usermessage" class="ui white quizmessage") 

		//- multiple choice buttons
		div(class="flexstretch" id="userinput" style="display: block;")
			div(class="flexitem moviename ui inverted button white") #{result[0].moviename}
			div(class="flexitem moviename ui inverted button white") #{result[1].moviename}
			div(class="flexitem moviename ui inverted button white") #{result[2].moviename}
			div(class="flexitem moviename ui inverted button white") #{result[3].moviename}

		//- Next move
		div(class="flexstretch" id="right-answer-block" style="display: none;")
			a(href="/" class="flexitem ui button green flexbutton") Guess Another
			a(href="/moviequotes" class="flexitem ui button green flexbutton") View Movie Quotes

		//- hidden field to capture user input
		input(id="user-guess" type="hidden" value=result[quiz].moviename)
	else
		div(class="flexcontainer")
			div(class="flexitem") 
				div(class="moviequotelarge" id="moviequote") Welcome to Name That Movie

	div(class="block")
		div(class="formcontainer")
			div(class="spacetop")
			div(class="gridcontainer")
				div(class="griditem default-grey")
					h2(class="blurbheader grey") The Story
					div(class="blurb") Screenwriting is about engaging an audience and getting those stories onto the page. To start a screenplay, you need a story. The story is everything: the plot, the characters, theme and the mood. The story is trying to say something, to give the audience something to think about, ponder. The characters are the vehicle for the story. There is no right or wrong way to write a screenplay. It is hard. The words needs to clearly present a coherent and compelling story.
				div(class="griditem default-grey")
					h2(class="blurbheader grey") Be part of the story
					div(class="blurb") Name That Movie is a collection of movie quotes. If you want to know who wrote “If you build it, he will come” from the movie Field of Dreams, you can find it here. If you have a favorite quote that is not in the collection you can add it by registering as a user. It is free to #[a(href="/register" class="link green") register.]
				div(class="griditem default-grey")
					h2(class="blurbheader grey") Want to be a Volunteer?
					div(class="blurb") Help validate the movie quotes collection so that it is verified and accurate. We rely on users to enter movie quotes and sometimes there will be mistakes or duplicates or even fakes. To maintain the integrity of the movie quotes collection so that everyone can enjoy it we need your help, and your love of words, to keep this in check. Click the button below to find out more.
					a(href="/" class="ui green mini button") Volunteer
						i(class="arrow alternate circle right large icon")
			div(class="gridcontainer")
				div(class="griditem default-grey")
					div(class="horizontal-line")
					div(class="landingicons")
						img(src="/images/icons8-film-reel-filled-50.png")
					div(class="statistics grey") 4
					div(class="statsticssubheading") Movie quotes added this week
				div(class="griditem default-grey")
					div(class="horizontal-line")
					div(class="landingicons")
						img(src="/images/icons8-clapperboard-50.png")
					div(class="statistics grey") 504
					div(class="statsticssubheading") Total movie quotes in the collection
				div(class="griditem default-grey")
					div(class="horizontal-line")
					div(class="landingicons")
						img(src="/images/icons8-movie-projector-50.png")
					div(class="statistics grey") 221
					div(class="statsticssubheading") Total movies added 
    
//- Add js script files  
script(src="/javascripts/landing.js" type="text/javascript")